@model DeleteEventViewModel

@{
	ViewBag.Title = "Delete the Event";
}

<div class="container">
	<div class="row justify-content-center">
		<div class="col-md-6">
			<div class="card">
				<div class="card-body text-center" style="color:black">
					<h2 class="card-title" style="margin-top: 15px;">@Model.Topic - @Model.Location</h2>
					<p class="text-danger mt-3">Are you sure you want to delete this event?</p>
				</div>
				<div class="card-footer">
					<form asp-action="DeleteConfirmed" method="post">
						<input type="hidden" asp-for="Id" />
						<button type="submit" class="btn btn-danger">Confirm Delete</button>
						<a asp-controller="Event" asp-action="All" class="btn btn-secondary">Cancel</a>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>

<style>
	body {
		background-color: #f8f9fa;
	}

	.container {
		margin-top: 50px;
	}

	.card {
		background-color: #fff;
		box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
	}

	.text-danger {
		color: #dc3545;
	}

	.btn-danger {
		background-color: #dc3545;
		border-color: #dc3545;
	}

	.btn-secondary {
		background-color: #6c757d;
		border-color: #6c757d;
	}
</style>